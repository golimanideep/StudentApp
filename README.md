# StudentApp
A StudentApp can be described as a simple web application that allows users (such as teachers or administrators) to perform CRUD (Create, Read, Update, Delete) operations on student data. These operations enable managing students' information, including personal details, grades, and enrollment statuses.
